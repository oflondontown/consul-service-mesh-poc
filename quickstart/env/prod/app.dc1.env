## Quickstart: dc1 application VM (legacy processes on the VM)
##
## Update these IPs for your environment.

CONSUL_DATACENTER=dc1

# The IP of THIS application VM (used in Consul service registrations + health checks).
HOST_IP=10.0.0.10

# LAN peers (Consul servers in this DC)
CONSUL_RETRY_JOIN=10.0.0.21

# Optional: Envoy image/flags
# ENVOY_IMAGE=docker.io/envoyproxy/envoy:v1.29-latest
# ENVOY_EXTRA_ARGS=--log-level info

# Optional demo (dc1 only): run an Envoy sidecar for an itch-consumer legacy process on port 9100
ENABLE_ITCH_CONSUMER=0

