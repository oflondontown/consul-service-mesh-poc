## Quickstart: dc2 application VM (legacy processes on the VM)
##
## Update these IPs for your environment.

CONSUL_DATACENTER=dc2

# The IP of THIS application VM (used in Consul service registrations + health checks).
HOST_IP=10.0.1.10

# LAN peers (Consul servers in this DC)
CONSUL_RETRY_JOIN=10.0.1.21

# Envoy image/flags
# (Set explicitly so your shell environment can't accidentally override it.)
ENVOY_IMAGE=docker.io/envoyproxy/envoy:v1.29-latest
ENVOY_EXTRA_ARGS=
