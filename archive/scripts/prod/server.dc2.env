## MVP example: dc2 Consul server VM
##
## Copy this file to scripts/prod/server.env on the dc2 Consul server VM.

CONSUL_DATACENTER=dc2
HOST_IP=10.0.1.21

# MVP: single Consul server per DC (no quorum). For production, set 3 and deploy 3 servers per DC.
CONSUL_BOOTSTRAP_EXPECT=1

# LAN peers (servers in this DC). With 1 server, it's just itself.
CONSUL_RETRY_JOIN=10.0.1.21

# WAN federation peers (servers in the other DC). With 1 server in dc1, it's that server IP.
CONSUL_RETRY_JOIN_WAN=10.0.0.21

# Optional (UI is bound to 127.0.0.1 by the Podman pod port mapping)
CONSUL_ENABLE_UI=1

# Optional: Envoy image/flags
# ENVOY_IMAGE=docker.io/envoyproxy/envoy:v1.29-latest
# ENVOY_EXTRA_ARGS=--log-level info
