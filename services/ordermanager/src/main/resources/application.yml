server:
  port: ${PORT:8081}

upstreams:
  refdata:
    base-url: ${REFDATA_BASE_URL:http://localhost:18082}

spring:
  main:
    banner-mode: "off"

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
